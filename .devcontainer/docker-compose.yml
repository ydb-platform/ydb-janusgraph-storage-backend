services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    init: true
    command: sleep infinity
    working_dir: /workspace
    # Share the YDB service's network namespace: no host ports are published
    # anywhere, so any number of devcontainer instances can run concurrently.
    network_mode: service:ydb
    volumes:
      - ..:/workspace:cached
      - m2-cache:/root/.m2
    environment:
      # Integration tests connect to the sidecar YDB by default; when these are
      # unset, tests fall back to spinning up their own container via Testcontainers.
      YDB_ENDPOINT: grpc://ydb:2136
      YDB_DATABASE: /local
      # Needed when Testcontainers is used from inside this container with the
      # host docker socket (docker-outside-of-docker): mapped ports live on the host.
      TESTCONTAINERS_HOST_OVERRIDE: host.docker.internal
    depends_on:
      ydb:
        condition: service_healthy

  ydb:
    # latest = current stable; use YDB_IMAGE=ydbplatform/local-ydb:nightly for the newest build
    image: ${YDB_IMAGE:-ydbplatform/local-ydb:latest}
    # local-ydb images are amd64-only; on Apple Silicon this runs via Rosetta.
    platform: linux/amd64
    hostname: ydb
    environment:
      YDB_USE_IN_MEMORY_PDISKS: "true"
      YDB_DEFAULT_LOG_LEVEL: NOTICE
      GRPC_PORT: "2136"
      GRPC_TLS_PORT: "2135"
      MON_PORT: "8765"
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 6<>/dev/tcp/localhost/2136'"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 30s

volumes:
  m2-cache:
